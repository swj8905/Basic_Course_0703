# 함수 정의 : 함수를 만듦 (definition)
def add(a, b): # a, b : 매개변수 (parameter)
    c = a + b
    return c # c : 반환값 (return value)

# 함수 호출 : 함수를 사용함 (call)
result = add(10, 20) # 10, 20 : 인자/인수 (argument)
print(result)